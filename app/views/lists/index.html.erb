<h2> Movie List Index </h2>
<p> Showing <%=@lists.count%> lists </p>

<div class="lists-container">
<% @lists.each do |list| %>
<div class="list-card">
<% if list.photo.key %>
  <%= cl_image_tag list.photo.key, height: 100, width: 198, crop: :fill %>
<% else %>
  <%= link_to (image_tag "test.jpg", size:"198x100", class: "list-poster"), list_path(list) %>
<% end %>
  <p>
<%= link_to list.name, list_path(list)%> (<%=list.movies.count%>)
</p>
</div>
<%end%>
</div>
<p> <%= link_to "New List", new_list_path %> </p>

<p> This VERY AGGRESSIVE cat is watching you. Stay out of trouble. </p>
<%= cl_image_tag("samples/animals/reindeer", width: 400, height: 300, crop: :fill) %>

<%# to display cloud image:
cl_image_tag @list.photo.keys, ize:"198x100", class: "list-poster" %>
